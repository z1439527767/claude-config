# CLAUDE.local.md

## 偏好
- PowerShell for Windows, Bash for POSIX
- 中文回复，代码/文档用英文
- 直接改文件，少解释
- 80% 方案现在 > 100% 方案以后
- 迭代节奏：保持核心不变，小步优化，不动根基

## 项目位置
- 四个项目已于 2026-06-24 全部删除，Projects 目录为空

## 当前任务
- 上次任务：四个项目已删除
- 配置状态：settings.json + CLAUDE.md + AGENTS.md 就位

## 自我质疑检查项
（每次动手前实际查，不是心理活动）
- 知识图谱有没有相关错误记录？→ mcp__memory__search_nodes
- 上次这个项目做对了什么？→ 查成功模式
- 这个改动最简单的方式是什么？少改一个文件行不行？
- 反过来想：如果不改代码，能不能达到同样效果？

## 最近发现
- 2026-06-24: 「进化自身」= 改自己的 hook/脚本/记忆/验证。「帮用户做事」= 动用户项目。两者完全不同。不要自作聪明扩展用户指令。

## 成功模式
（完成得好、做得对的事情，下次可以直接复用）
- 三文件體系：只改 settings.json + CLAUDE.md + AGENTS.md 就能完整控制行為
- 記憶樹清理：先審計 → 分類 → 批量刪 → 更新索引 → 驗證
- 思考流程閉環：回憶 → 執行 → 驗證 → 沉澱

## 永久教训
- 用户说"清理" = 删多余文件，不是建系统
- 用户说"太复杂" = 删到最简单，不是解释
- 用户说"记到行为里" = 改 CLAUDE.md / AGENTS.md
- 行为规则用负面约束，不用正面指令
- 框架 > 零散修复。改之前先想影响范围
- 配置 = settings.json + CLAUDE.md + AGENTS.md，三个文件足够
- 「进化你自己」≠ 「修我的项目」。别自作聪明扩展用户指令。
