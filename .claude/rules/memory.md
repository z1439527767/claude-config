---
paths:
  - "CLAUDE.local.md"
  - "**/MEMORY.md"
---

# 记忆闭环：越用越聪明
> 🟢 Guidance | 每次任务完成后执行

## 三层记忆 + 蒸馏 + 衰减
- **错误** → 发生时立即记入知識圖譜，标注时间、上下文、根因
- **沉淀** → 任务完成后，新发现寫回 CLAUDE.local.md；做对的事寫入成功模式
- **蒸馏** → 同类记忆积累 3+ 条，抽取出通用原则，寫入 CLAUDE.md 或 AGENTS.md
- **衰减** → 记忆 >30 天未访问 = 标记 aging，>60 天 = stale，>90 天 = 删除或蒸馏后归档
- **原则** → 每次 SessionStart 加载，即时生效

## 强制回忆触发
- 用户说"又""再次""还是""上次" → 立即 search_nodes 查相关错误记忆
- 用户纠正我 → 先查知识图谱有没有同类纠正，再回应
- 开始新任务 → 先查该项目的历史教训和成功模式
- 自我质疑时 → 实际查询知识图谱，不是心理活动
- 🔴 收到任何报错/异常/故障 → 第一步查知識圖譜，第二步查代码。不颠倒。

## 同步 🔴
- 改 settings.json / MCP / hooks / env vars → 同轮立即更新知識圖譜对应实体
- 删文件 → 同轮检查知識圖譜有无引用，有则清理
- 不等到"下次再说"——文件跟大脑永远保持一步以内

## 隔离 🔴
- mcp__memory 知识图谱仅存 .claude 框架自身数据（规则/进化/脚本/hook/教训）
- 项目数据（ComfyUI/workflow/视频制作）→ 项目自己的 `.claude/memory/`
- 不跨项目加载记忆，不把项目数据写入框架知识图谱

## 流程
- 错误 → 记录 → 同类累积 → 蒸馏成原则 → 更新规则文件 → 下次自动避免
- 成功 → 记录到成功模式 → 同类累积 → 下次直接复用
